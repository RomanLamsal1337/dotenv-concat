name: dotenv-concat
description: "Concat multiple dotenv files via comma-separated list"
inputs:
  paths:
    required: true
    description: "Comma separated list of .env files to concat. Will be merged in order."
runs:
  using: node12
  main: "build/main.js"
